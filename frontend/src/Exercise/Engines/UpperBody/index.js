import { pushup } from "./pushup";
import { bicep_curl } from "./bicep_curl";
import { overhead_press } from "./overhead_press";
import { single_arm_row } from "./single_arm_row";
import { tricep_extension } from "./tricep_extension";

export const UpperBodyEngines = {
  pushup,
  bicep_curl,
  overhead_press,
  single_arm_row,
  tricep_extension,
};
